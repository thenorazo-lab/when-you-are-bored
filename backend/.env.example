# MongoDB 연결 URI (MongoDB Atlas 무료 클러스터 사용 권장)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/when-bored?retryWrites=true&w=majority

# 포트 설정 (Render에서 자동 설정됨)
PORT=5000

# 프론트엔드 URL (CORS 설정용)
FRONTEND_URL=https://your-vercel-app.vercel.app
